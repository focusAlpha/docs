---
sidebarTitle: "Connect MCP"
title: "Connect via MCP"
description: "Use FocusAlpha from Claude, Cursor, or any MCP client — every dataset exposed as a tool, authenticated with your API key."
---

FocusAlpha ships a hosted [Model Context Protocol](https://modelcontextprotocol.io) server. Point an MCP-capable client at it and the model can search transcripts, pull financials, screen companies, and watch events — every dataset in the API, as tools.

```text
https://mcp.focusalpha.ai/mcp
```

The server speaks the HTTP transport and is stateless. Authentication is your normal API key, sent as a header on every request:

```text
Authorization: Bearer fa_live_...
```

<Note>
  MCP tool calls are ordinary API calls underneath. They spend the same credits, respect the same plan entitlements, and count against the same rate limits as REST requests — see [Plans & credits](/concepts/plans-credits). A missing or invalid key is rejected with `401` before any data is read.
</Note>

<Info>
  This is the **data** server. Separately, `https://docs.focusalpha.ai/mcp` serves this documentation site itself over MCP (search and read the docs) — no API key required. Connect both if you want an agent that can look up the docs *and* pull the data.
</Info>

## Claude Code

```bash
claude mcp add --transport http focusalpha https://mcp.focusalpha.ai/mcp \
  --header "Authorization: Bearer fa_live_your_key_here"
```

## Claude Desktop

Add to `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "focusalpha": {
      "type": "http",
      "url": "https://mcp.focusalpha.ai/mcp",
      "headers": { "Authorization": "Bearer fa_live_your_key_here" }
    }
  }
}
```

## Cursor

Add to `.cursor/mcp.json` (project) or `~/.cursor/mcp.json` (global):

```json
{
  "mcpServers": {
    "focusalpha": {
      "url": "https://mcp.focusalpha.ai/mcp",
      "headers": { "Authorization": "Bearer fa_live_your_key_here" }
    }
  }
}
```

## Other clients

Any MCP client that supports the HTTP transport and can attach an `Authorization` header works the same way — configure the URL and the header, nothing else is required.

<Warning>
  Your API key travels in the client configuration. Keep the config file out of source control, and revoke the key in the [dashboard](https://app.focusalpha.ai) if it leaks.
</Warning>

## Available tools

Tools map one-to-one onto the REST API. The model can also call `list_datasets` and `list_coverage` at any time to discover what is available and how fresh it is.

| Dataset | Tools | Plan | REST docs |
| --- | --- | --- | --- |
| Earnings call transcripts | `retrieve`, `list_documents`, `get_document`, `get_document_segments` | Free trial | [Semantic retrieval](/transcripts/semantic-retrieval) |
| Agent-ready indexed filings | `get_filings`, `get_filing_items` | Free trial | [Filings](/filings/index), [Filing items](/filings/items) |
| Financial statements | `get_income_statements`, `get_balance_sheets`, `get_cash_flow_statements`, `get_all_financials` | Free trial | [Financials](/financials/combined) |
| Financial metrics | `get_financial_metrics` | Free trial | [Financial metrics](/financials/metrics) |
| Segmented financials | `get_segmented_financials` | Free trial | [Segments](/financials/segments) |
| Company facts | `get_company_facts` | Free trial | [Company facts](/financials/company-facts) |
| Company search & profile | `search_companies`, `get_company` | Free trial | [Company registry](/companies/registry) |
| Discovery | `list_datasets`, `list_coverage` | Free trial | [Coverage & freshness](/concepts/coverage) |
| Institutional holdings (13F) | `get_institutional_holdings` | Professional | [13F holdings](/ownership/13f) |
| Insider trades | `get_insider_trades` | Professional | [Insider trades](/ownership/insider-trades) |
| Executive compensation & proxy ownership | `get_executive_compensation`, `get_proxy_ownership` | Professional | [Executive compensation](/ownership/executive-compensation), [Proxy ownership](/ownership/proxy-ownership) |
| Short interest | `get_short_interest` | Professional | [Short interest](/ownership/short-interest) |
| Adviser directory & profile | `search_institutions`, `get_institution_profile` | Professional | [Form ADV directory](/advisers/form-adv), [ADV brochure](/advisers/adv-brochure) |
| Adviser screener | `screen_institutions` | Professional | [Adviser screening](/advisers/screening) |
| ETF directory & profile | `search_etfs`, `get_etf`, `get_etf_taxonomy` | Professional | [ETF data](/etf/overview), [Taxonomy](/etf/taxonomy) |
| ETF daily series & holdings | `get_etf_history`, `get_etf_holdings` | Professional | [NAV & shares](/etf/nav), [Holdings](/etf/holdings) |
| Market data & benchmarks | `get_market_data`, `get_benchmark_prices` | Professional | [Prices](/market-data/prices), [Benchmarks](/market-data/benchmarks) |
| Taiwan datasets | `get_taiwan_financials`, `get_taiwan_monthly_revenue`, `get_taiwan_foreign_ownership` | Professional | [Taiwan financials](/international/taiwan-financials) |
| Korea datasets | `get_korea_financials`, `get_korea_report_sections` | Professional | [Korea financials](/international/korea-dart-financials) |
| Form 20-F | `get_adr_20f_notes`, `get_adr_20f_data` | Professional | [Form 20-F](/international/form-20f) |
| Company screener | `screen_companies` | Fund | [Company screening](/companies/screening) |
| ETF screener | `screen_etfs` | Fund | [ETF screening](/etf/screening) |
| Real-time company events | `get_company_events` | Fund | [News events](/news/news-events) |
| Real-time company news | `get_company_news` | Fund | [Company news](/news/company-news) |
| Real-time macro news | `get_macro_news` | Fund | [News themes](/news/themes) |
| Real-time US events and filings | `get_8k_events`, `get_8k_raw_filings` | Fund | [8-K events](/events/8k-events), [8-K exhibits](/events/8k-exhibits) |
| Real-time guidance and changes | `get_guidance`, `get_guidance_changes` | Fund | [Guidance](/events/guidance), [Guidance changes](/events/guidance-changes) |
| Real-time earnings result analysis | `get_8k_earnings_results` | Fund | [Earnings results](/events/earnings-results) |
| Prediction-market consensus | `get_prediction_markets_quote`, `get_prediction_markets_consensus` | Fund | [Prediction markets](/events/prediction-markets), [Earnings consensus](/events/earnings-consensus) |
| Real-time international disclosures | `get_international_disclosure` | Fund | [Global disclosures](/international/exchange-disclosures) |
| VC investor search | `search_vc_investors` | Fund | [Venture investors](/advisers/venture-investors) |

Datasets your plan does not include return a clear upgrade message rather than data — the model will tell you which tier a tool needs.

## Related pages

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Creating and managing the API key your MCP client uses.
  </Card>
  <Card title="Plans & credits" icon="coins" href="/concepts/plans-credits">
    What each plan unlocks and how tool calls are metered.
  </Card>
</CardGroup>
